import { BorrowerDashboardDirective } from './borrower-dashboard.directive';

describe('BorrowerDashboardDirective', () => {
  const directive = new BorrowerDashboardDirective(null);

  it('should create an instance', () => {
    expect(directive).toBeTruthy();
  });
});
