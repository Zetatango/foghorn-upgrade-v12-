# Auto-Correct Style Guide

Install rubocop (style auto-correct) hooks into the .git/hooks/pre-commit

```
rake rubocop:install
```

You can also install rubocop as a gem for use within RubyMine. See instructions [here](https://www.jetbrains.com/help/ruby/robocop.html).